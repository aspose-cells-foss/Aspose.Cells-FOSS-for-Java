package com.aspose.cells_foss;

/**
 * Represents the visibility type of a worksheet.
 */
public enum VisibilityType {
    /** The worksheet is visible. */
    VISIBLE,
    /** The worksheet is hidden. */
    HIDDEN,
    /** The worksheet is very hidden. */
    VERY_HIDDEN
}