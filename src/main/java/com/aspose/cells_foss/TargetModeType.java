package com.aspose.cells_foss;

/**
 * Represents the target mode type for cell references.
 */
public enum TargetModeType {
    /** External target mode. */
    EXTERNAL,
    /** File path target mode. */
    FILE_PATH,
    /** Email target mode. */
    EMAIL,
    /** Cell reference target mode. */
    CELL_REFERENCE
}